#include "glimac/BlockTypes.hpp"
#include <iostream>

namespace glimac {
    Color BlockTypes::colors[] = {
        Color(127, 127, 127), // stone
        Color(0, 255, 0), // dirt
        Color(255, 242, 0), // sand
        Color(34, 177, 76), // leaves
        Color(0, 0, 0), // bedrock
        Color(128, 64, 0) // wood
    };
}
